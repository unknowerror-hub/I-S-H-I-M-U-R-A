<?php
/**
 * ==============================================================================
 * SYSTEM ISHIMURA — GLOBAL WEB CONFIGURATION
 * ==============================================================================
 * Описание: Глобальные константы окружения, авторизации и подключения к СУБД.
 * ==============================================================================
 */

define('DB_HOST', '127.0.0.1');
define('DB_PORT', '5432');
define('DB_NAME', 'ishimura_db');
define('DB_USER', 'ishimura_admin');

// Пароль подставляется автоматически при выполнении скрипта install.sh
define('DB_PASS', 'Nh0uk0lbn@'); 

// Настройки безопасности сессий
ini_set('session.cookie_httponly', 1);
ini_set('session.use_only_cookies', 1);
ini_set('session.cookie_secure', 0); // Установите 1, если используется HTTPS
